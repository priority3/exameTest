# Used by docker-compose.prod.yml

# Public domain (Caddy will request TLS certs for it).
DOMAIN=example.com

# Postgres
POSTGRES_PASSWORD=change_me

# Images
IMAGE_TAG=main

# LLM (optional until you enable generation/grading)
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_CHAT_MODEL=gpt-4.1-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

